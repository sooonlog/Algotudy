// 다음 중 set을 만드는 방법으로 올바른 것을 모두 고르시오.

// 1)  var x = {1, 2, 3, 5, 6, 7};
// 2)  var x = {};
// 3)  var x = new Set('javascript');
// 4)  var x = new Set(range(5));
// 5)  var x = new Set();

// 정답: 3,5
// set 객체는 값 콜렉션이다. 삽입순서대로 요소순회가 가능하다. 값은 콜렉션 내에서 유일해야한다.